apiVersion: v1
kind: Service
metadata:
  name: {{ include "eu-regulations-mcp.fullname" . }}
  labels:
    {{- include "eu-regulations-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "eu-regulations-mcp.selectorLabels" . | nindent 4 }}
